<UserControl x:Class="InnoWidget.Widgets.Hardware.HardwareWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="160" d:DesignWidth="260">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" Style="{StaticResource AppText.Title}" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="CPU" Style="{StaticResource AppText.Label}" />
                <ProgressBar Height="8" Minimum="0" Maximum="100" Value="{Binding CpuPercent, Mode=OneWay}" Margin="0,6,0,0" />
                <TextBlock Text="{Binding CpuPercent, StringFormat={}{0:0}%}" Foreground="{DynamicResource AppBrush.TextPrimary}" FontSize="18" FontWeight="SemiBold" Margin="0,6,0,0" />
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="RAM" Style="{StaticResource AppText.Label}" />
                <ProgressBar Height="8" Minimum="0" Maximum="100" Value="{Binding RamPercent, Mode=OneWay}" Margin="0,6,0,0" />
                <TextBlock Text="{Binding RamPercent, StringFormat={}{0:0}%}" Foreground="{DynamicResource AppBrush.TextPrimary}" FontSize="18" FontWeight="SemiBold" Margin="0,6,0,0" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
