<UserControl x:Class="InnoWidget.Widgets.Media.MediaWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="220" d:DesignWidth="320">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <svgc:SvgViewbox Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Center" Source="pack://application:,,,/Assets/Icons/media.svg" />
            <TextBlock Grid.Column="2" Text="{Binding Title}" Style="{StaticResource AppText.Title}" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="72" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Width="72" Height="72" CornerRadius="12" Background="{DynamicResource AppBrush.Surface2}" BorderBrush="{DynamicResource AppBrush.Border}" BorderThickness="1" ClipToBounds="True">
                <Image Source="{Binding Cover}" Stretch="UniformToFill" />
            </Border>

            <StackPanel Grid.Column="2">
                <TextBlock Text="{Binding TrackTitle}" Foreground="{DynamicResource AppBrush.TextPrimary}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                <TextBlock Text="{Binding Artist}" Foreground="{DynamicResource AppBrush.TextSecondary}" FontSize="12" TextTrimming="CharacterEllipsis" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="4" Background="{DynamicResource AppBrush.Surface2}" BorderBrush="{DynamicResource AppBrush.Border}" BorderThickness="1" CornerRadius="14" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource AppBrush.TextMuted}" FontSize="11" Text="Kontroller" />

                <Button Grid.Column="1" Style="{StaticResource MediaControlButton}" Command="{Binding PrevCommand}" Margin="0,0,8,0">
                    <materialDesign:PackIcon Kind="SkipPrevious" Width="20" Height="20" />
                </Button>
                <Button Grid.Column="2" Style="{StaticResource MediaControlButton}" Command="{Binding PlayPauseCommand}" Margin="0,0,8,0">
                    <materialDesign:PackIcon Kind="PlayPause" Width="20" Height="20" />
                </Button>
                <Button Grid.Column="3" Style="{StaticResource MediaControlButton}" Command="{Binding NextCommand}">
                    <materialDesign:PackIcon Kind="SkipNext" Width="20" Height="20" />
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
