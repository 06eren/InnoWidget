<UserControl x:Class="InnoWidget.Widgets.Weather.WeatherWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="180" d:DesignWidth="280">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="24" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <svgc:SvgViewbox Width="20" Height="20" Stretch="Uniform" VerticalAlignment="Center" Source="pack://application:,,,/Assets/Icons/cloud.svg" />
            <TextBlock Grid.Column="2" Text="Hava Durumu" Style="{StaticResource AppText.Title}">
                <TextBlock.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.85" To="1" Duration="0:0:2.8" AutoReverse="True" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </TextBlock.Triggers>
            </TextBlock>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="12" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="12" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <svgc:SvgViewbox Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Center" Source="pack://application:,,,/Assets/Icons/cloud.svg" Margin="0,0,6,0" />
                    <TextBlock Text="Sıcaklık" Style="{StaticResource AppText.Label}" />
                </StackPanel>
                <TextBlock Text="{Binding Temperature}" Foreground="{DynamicResource AppBrush.TextPrimary}" FontSize="24" FontWeight="SemiBold" Margin="0,6,0,0">
                    <TextBlock.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.7" To="1" Duration="0:0:2" AutoReverse="True" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </TextBlock.Triggers>
                </TextBlock>
                <TextBlock Text="{Binding Description}" Foreground="{DynamicResource AppBrush.TextSecondary}" FontSize="14" Margin="0,2,0,0" />
            </StackPanel>

            <StackPanel Grid.Row="2">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <svgc:SvgViewbox Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Center" Source="pack://application:,,,/Assets/Icons/world.svg" Margin="0,0,6,0" />
                    <TextBlock Text="Nem" Style="{StaticResource AppText.Label}" />
                </StackPanel>
                <TextBlock Text="{Binding Humidity}" Foreground="{DynamicResource AppBrush.TextPrimary}" FontSize="14" FontWeight="SemiBold" Margin="0,6,0,0" />
            </StackPanel>

            <StackPanel Grid.Row="4">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <svgc:SvgViewbox Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Center" Source="pack://application:,,,/Assets/Icons/network.svg" Margin="0,0,6,0" />
                    <TextBlock Text="Rüzgar" Style="{StaticResource AppText.Label}" />
                </StackPanel>
                <TextBlock Text="{Binding WindSpeed}" Foreground="{DynamicResource AppBrush.TextPrimary}" FontSize="14" FontWeight="SemiBold" Margin="0,6,0,0" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
