<UserControl x:Class="InnoWidget.Widgets.SystemInfo.SystemWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="320">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="24" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <svgc:SvgViewbox Width="20" Height="20" Stretch="Uniform" VerticalAlignment="Center" Source="pack://application:,,,/Assets/Icons/system.svg" />
            <TextBlock Grid.Column="2" Text="Sistem Bilgileri" Style="{StaticResource AppText.Title}">
                <TextBlock.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.85" To="1" Duration="0:0:2.5" AutoReverse="True" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </TextBlock.Triggers>
            </TextBlock>
        </Grid>

        <StackPanel Grid.Row="2">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
                <svgc:SvgViewbox Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Center" Source="pack://application:,,,/Assets/Icons/computer.svg" Margin="0,0,6,0" />
                <TextBlock Text="İşletim Sistemi" Style="{StaticResource AppText.Label}" />
            </StackPanel>
            <TextBlock Text="{Binding OSName}" Foreground="{DynamicResource AppBrush.TextPrimary}" FontSize="12" Margin="0,0,0,12" />
            
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
                <svgc:SvgViewbox Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Center" Source="pack://application:,,,/Assets/Icons/settings.svg" Margin="0,0,6,0" />
                <TextBlock Text="Sürüm" Style="{StaticResource AppText.Label}" />
            </StackPanel>
            <TextBlock Text="{Binding OSVersion}" Foreground="{DynamicResource AppBrush.TextPrimary}" FontSize="12" Margin="0,0,0,12" />
            
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
                <svgc:SvgViewbox Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Center" Source="pack://application:,,,/Assets/Icons/widget.svg" Margin="0,0,6,0" />
                <TextBlock Text="Bilgisayar" Style="{StaticResource AppText.Label}" />
            </StackPanel>
            <TextBlock Text="{Binding ComputerName}" Foreground="{DynamicResource AppBrush.TextPrimary}" FontSize="12" Margin="0,0,0,12" />
            
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
                <svgc:SvgViewbox Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Center" Source="pack://application:,,,/Assets/Icons/note.svg" Margin="0,0,6,0" />
                <TextBlock Text="Kullanıcı" Style="{StaticResource AppText.Label}" />
            </StackPanel>
            <TextBlock Text="{Binding UserName}" Foreground="{DynamicResource AppBrush.TextPrimary}" FontSize="12" Margin="0,0,0,12" />
            
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
                <svgc:SvgViewbox Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Center" Source="pack://application:,,,/Assets/Icons/clock.svg" Margin="0,0,6,0" />
                <TextBlock Text="Çalışma Süresi" Style="{StaticResource AppText.Label}" />
            </StackPanel>
            <TextBlock Text="{Binding Uptime}" Foreground="{DynamicResource AppBrush.TextPrimary}" FontSize="12" FontWeight="SemiBold">
                <TextBlock.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.7" To="1" Duration="0:0:1.8" AutoReverse="True" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </TextBlock.Triggers>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
